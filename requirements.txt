fastapi==0.115.0
uvicorn>=0.31.1
python-multipart==0.0.9
aiofiles==23.2.1
aiohttp>=3.11.18
httpx==0.28.1
python-dotenv==1.0.1
 
# Supabase
supabase==2.15.0
 
# OpenAI
openai==2.26.0
 
# Parsing libraries
mammoth==1.7.1          # docx
openpyxl==3.1.4         # xlsx - use read_only mode
# tiktoken removed — no pre-built wheels for Python 3.14 on Render
# using character-based token estimation instead (4 chars ≈ 1 token)
 
# Utilities
# hashlib is built into Python stdlib — no install needed
tenacity==8.3.0         # retry logic for API calls
PyJWT>=2.10.1           # JWT verification for chat endpoint
openai-agents==0.12.4   # OpenAI Agents SDK for agentic RAG